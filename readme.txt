* For local http server. To make your machine a local server.
https://www.npmjs.com/package/http-server
1.npm install http-server -g
This will install http-server globally so that it may be run from the command line.
2.http-server [path] [options]
path needed if you are not on a project folder.
or
C:\sonia\node-project\Node-Get-Started\1.4-first-app\JavaScript-DOM-Tutorial>http-server

* when we use var elm= querySelectorAll then we do not need to convert it to array. but if we use querySelector or
getElementsByClassName,getElementsById
then we need to convert into array . for example Array.from(elm).

* The Document Object Model is a cross-platform and language-independent interface that treats an XML or HTML document
 as a tree structure wherein each node is an object representing a part of the document.
  The DOM represents a document with a logical tree.
